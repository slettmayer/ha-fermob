# Deliberately lean: protocol.py imports nothing from Home Assistant, so the
# test suite needs neither HA nor pytest-homeassistant-custom-component. Add the
# latter when the first test needs a real hass instance.
pytest>=9.1.1
cryptography>=49.0.0
